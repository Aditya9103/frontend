/**
 * setupTests.js — Frontend Vitest global setup
 * Imports @testing-library/jest-dom matchers so every test file can use
 * toBeInTheDocument(), toHaveTextContent(), etc. without explicit imports.
 */
import '@testing-library/jest-dom';
